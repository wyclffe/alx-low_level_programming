#include "main.h"
/**
 * print_square - make a square
 * @size: length of square
 * Return: void
*/
	void print_square(int size)
{
		int i, j;
		if (n > 0)
		{

		for (i = 0; i < size; i++)
	{
		for (j = 0 ; j < size; j++)
		{
			putchar('#');
		}
		putchar('\n');
	}
		putchar('\n');
		}
		else
			putchar('\n');

}
